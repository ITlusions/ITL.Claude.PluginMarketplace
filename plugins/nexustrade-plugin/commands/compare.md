---
description: Side-by-side comparison of two tickers using the NexusTrade framework
argument-hint: [TICKER1] [TICKER2]
---

Use the `nexustrade` skill to produce a side-by-side comparison of:

$ARGUMENTS

For each ticker, gather bias (LONG/SHORT/NEUTRAL), key technical levels (price, 50/200MA, RSI, MACD, support/resistance), float/short/dilution red flags, a trade setup (entry/stop/targets), and a risk score with verdict (TRADE/PASS/WATCH). Present the comparison as a single table for quick scanning, then give a direct recommendation on which setup is stronger and why — no "it depends." End with the educational-only disclaimer.
